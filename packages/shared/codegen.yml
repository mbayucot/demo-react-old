overwrite: true
schema: "http://localhost:4000/graphql"
generates:
  ./src/types/index.d.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
  ./graphql.schema.json:
    plugins:
      - "introspection"
